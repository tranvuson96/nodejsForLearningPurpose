<%- include('includes/head.ejs') %>
</head>

<body>
    <%- include('includes/navigation.ejs') %>
    <% if (isAuthenticated) { %> 
        
        <h1>Chào mừng <%= role === 'admin' ? user.username : user.name %></h1>
    <% } %>
    <% if (!isAuthenticated) { %> 
        <h1>Vui lòng đăng nhập để tiếp tục</h1>
        
    <% } %>

<%- include('includes/end.ejs') %>